import 'package:flutter/material.dart';

class OrganisationProfile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Organisation profile : Steve"),
    );
  }
}